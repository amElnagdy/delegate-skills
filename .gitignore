# OS / editor cruft
.DS_Store
*.swp

# Node
node_modules/

# relay.mjs run artifacts, if a user points --out-dir back into the repo
.delegate/

# smoke fixtures for delegate-setup (created under the repo tree)
.tmp-fleet-smoke-*/

# local agent config symlink (see AGENTS.md)
CLAUDE.md
